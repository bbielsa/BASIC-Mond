fun run(this) {
	if(this.expression)
		printLn(this.expression.evaluate());
	else
		printLn("\n");
}

fun new(expression) {
	return {
		expression: expression,
		type: "print",
		run: run
	}.enableThis();
}

return {new};
